version: '3.9'

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: backend
    ports:
      - "3000:3000"

    env_file:
      - backend/.env

    volumes:
      - ./backend:/app
      - /app/node_modules
      
    restart: unless-stopped